using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;

namespace speedball.core.RecursionAndDynamicProgramming
{
    public static class RobotPathFinder
    {
        public static bool GetPath(int x, int y, List<Point> path)
        {
            //Point p = new Point(x, y);
            //path.Add(p);

            //if (x == 0 && y == 0) return true; // found a path

            //bool success = false;
            //if (x >= 1 && ) 

            return false;
        }
    }
}
